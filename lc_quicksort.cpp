#include <iostream>
#include <vector>


class Solution{
	public:
		int quicksort(vector<int> &nums, int left, int right){
			if (left<right){
				int key, i, j;

				i=left;
				j=right;
				key=nums[left];
				while (i<j){
					while (i<j && nums[j])
				}
			}
		}
};

int main (){
	return 0;
}
