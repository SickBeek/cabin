#include <iostream>
#include <string>
#include <vector>
using namespace std;

class Solution{
	public:
		
		vector<vector<int>> rotate_layers(vector<vector<int>> &grid, int k){
			int i,j, row, col, tmp, layer;

			layer=

		}
};

int main (){
	return 0;
}
